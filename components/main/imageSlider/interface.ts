export interface SlideProps {
  width: number;
  transitionSwitch?: boolean;
  left?: number;
  imagePath?: string;
}

export interface eventBox {
  path: string;
  title: string;
  subTitle?: string;
}
